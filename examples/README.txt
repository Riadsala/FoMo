To fit models: use the 1_fit_models folder and the information in the general README file to fit models to a dataset.

We also provide a script to make model plots for any model version and dataset you want to generate graphs for. This is the 'script_to_make_model_plots.R' script, and it uses the QMD in the helper folder to generate HTML files in the 'reports' folder.

